# Environment configuration example (copy to .env and fill in secrets)

# ===== OpenAI Translation =====
OPENAI_API_KEY=YOUR_API_KEY
# Optional: custom base URL (default https://api.openai.com/v1)
OPENAI_BASE_URL=https://api.302.ai/v1/chat/completions
# Model name used for translation
MODEL_NAME=gemini-3-pro-preview
# Optional: HTTP proxy for both download and LLM calls
# PROXY=http://127.0.0.1:7890

# ===== Download =====
# Optional: direct video URL; can also pass via CLI --url
VIDEO_URL=
# Optional: start from local files instead of downloading
LOCAL_VIDEO=
LOCAL_SRT=
DOWNLOAD_DIR=downloads
# Subtitle languages (comma-separated or "all")
SUB_LANGS=en
# Download auto-generated subtitles (0/1)
AUTO_SUBS=1
# Download subtitles only, skip video (0/1)
SUBS_ONLY=0
# yt-dlp format selector
YTDLP_FORMAT=bestvideo+bestaudio/best
# Optional: extra extractor args for yt-dlp
EXTRACTOR_ARGS=
# Optional: load cookies from browser (chrome/edge/firefox)
COOKIES_FROM_BROWSER=
# Optional: cookies file (Netscape format)
COOKIES_FILE=cookies.txt

# ===== Translation =====
TRANSLATE_DIR=translate
TARGET_LANGUAGE=Chinese

# ===== TTS =====
TTS_INPUT_DIR=input
TTS_OUTPUT_DIR=output
TTS_SPEAKER=cxk
TTS_DAC_CHECKPOINT=checkpoints/openaudio-s1-mini/codec.pth
TTS_T2S_CHECKPOINT=checkpoints/openaudio-s1-mini
TTS_DEVICE=cuda
TTS_TEMPERATURE=0.8
TTS_TOP_P=0.8
TTS_REPETITION_PENALTY=1.1
TTS_SEED=42
# Precision: 1 to force float16, 0 for bfloat16
TTS_HALF=0
# Enable torch.compile (0/1)
TTS_COMPILE=0

# ===== Compose =====
FINAL_OUTPUT_DIR=final
# Audio mode: replace (tts only), mix (tts+lowered original), bgm (tts+bgm only)
AUDIO_MODE=replace
ORIGINAL_VOLUME=0.2
TTS_VOLUME=1.0
# Burn subtitles into video (0/1)
BURN_SUBS=1
# Overlap strategy: truncate | speed | hybrid | none
OVERLAP_STRATEGY=hybrid
# Max speedup when overlap strategy uses speed/hybrid
MAX_TTS_SPEED=1.5
# Vocal separation method if used: demucs | spleeter
SEPARATION_METHOD=demucs

# ===== Runtime =====
TEMP_DIR=temp
LOG_DIR=logs
# Isolate each run into timestamped subdirs (0/1)
ISOLATE_RUN=1
# Minimum chars to keep a short line before merging
SRT_MIN_CHARS=0
